# Versiones mínimas con ruedas precompiladas para Python 3.13 en Windows (evita MSVC/Rust).
fastapi>=0.109.0
sqlmodel>=0.0.14
uvicorn[standard]>=0.27.0
psycopg2-binary>=2.9.10
python-dotenv>=1.0.0
pydantic[email]>=2.10.0
pydantic-settings>=2.2.0
# Parcial 2 — auth (JWT cookie, login form, bcrypt)
python-jose[cryptography]==3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=3.2.0,<4.0.0
python-multipart>=0.0.9
# Global — Cloudinary (imagenes de productos)
cloudinary>=1.40.0
# tests (bonus): unitarios de logica de negocio
pytest>=8.0.0
